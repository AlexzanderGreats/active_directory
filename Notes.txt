Commands: 

    ```shell
    $creds = (Get-Credential)
    ```
MDA\Administrator — P@ssw0rd123!

    ```shell
    $dc = New-PSSession 192.168.244.155 -Credential $creds
    ```

    ```shell
    > $session = New-PSSession $dc -Credential $cred
    ```

    ```shell
    Copy-Item `

    >>     -FromSession $session `

    >>     -Path "C:\Windows\Tasks\[filename]" `

    >>     -Destination "C:\Users\Local_Admin\Documents\GitHub\active_directory\Files\[filename]"
    ```
